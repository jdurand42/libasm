#include <string.h>

size_t	ft_write(size_t fd, const char *s, size_t size);
size_t	ft_read(size_t fd, char *buffer, size_t buffer_size);
char	*ft_strcpy(char *dest, char *src);
void	ft_bzero(void *s, size_t n);
